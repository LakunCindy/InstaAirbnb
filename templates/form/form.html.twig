{% extends 'base.html.twig' %}

{% block title %}Hello TaskController!{% endblock %}

{% block header %}
    {{ include('generic/header.html.twig') }}
{% endblock %}

{% block body %}
    {{ form_start(form) }}
    {{ form_row(form.title) }}
    {{ form_row(form.content) }}
    {{ form_row(form.price) }}
    {{ form_row(form.submit) }}
    {{ form_end(form) }}
{% endblock %}
